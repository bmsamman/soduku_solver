include Sudoku
describe Stepper do
  it 'clones an existing board'
  it 'finds the next possible step'
  it 'steps through a board until an invalid step is found'
  it 'goes back one step'
  it 'checks if the board is solvable'


  it 'does not execute if no possible values'
  it 'does not execute if no possible values'
end
